# Tek Sayfa Chatbot (GitHub Pages) + Backend (Render) + Drive Log

## Kısaca
- `client/` = GitHub Pages (tek sayfa)
- `server/` = Render (OpenAI API key gizli + Google Drive'a log)

## Kurulum adımları ChatGPT mesajında step-by-step anlatıldı.
